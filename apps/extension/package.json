{
  "name": "postflow-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/engagement.test.cjs tests/publishing-detection.test.cjs",
    "build": "npm run build:dev",
    "build:dev": "tsc && node scripts/copy-static.mjs development",
    "build:prod": "tsc && node scripts/copy-static.mjs production",
    "watch": "npm run build:dev && tsc --watch"
  },
  "engines": {
    "node": ">=20.12.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "linkedom": "^0.18.13",
    "typescript": "^5.0.0"
  }
}
